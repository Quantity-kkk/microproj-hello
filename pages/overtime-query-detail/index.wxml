<!--pages/overtime-query-detail/index.wxml-->
<table 
    header-row-class-name="header-class"
    cell-class-name="content-close"
    headers="{{tableHeader}}" 
    data="{{ row }}" 
    stripe="{{ stripe }}" 
    border="{{ border }}" 
    no-data-msg="{{ msg }}"
    bind:rowClick="onRowClick"
  />